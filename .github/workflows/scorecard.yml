name: OpenSSF Scorecard

on:
  push:
    branches: [master]
  schedule:
    - cron: '23 6 * * 1' # Weekly on Monday, in UTC.

permissions:
  contents: read

jobs:
  analysis:
    name: Scorecard analysis
    if: github.repository == 'ldbumble/taskuary'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      security-events: write # Upload findings to GitHub code scanning.
      id-token: write # Authenticate public results for the README badge.
    # Publishing permits only Scorecard's approved actions in this job.
    # Keep application builds and arbitrary shell steps in the separate CI workflow.
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run Scorecard
        uses: ossf/scorecard-action@2d1146689b8cda280b9bc96326124645441f03bc # v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true

      - name: Upload results artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: scorecard-results
          path: results.sarif
          retention-days: 5

      - name: Upload findings to code scanning
        uses: github/codeql-action/upload-sarif@b96794f015dfd88f77b49b1c93e0fa7110f94c63 # v4.38.0
        with:
          sarif_file: results.sarif
